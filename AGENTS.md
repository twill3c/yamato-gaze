# AGENTS.md — yamato-gaze

和辻哲郎『古寺巡礼』(1919)・亀井勝一郎『大和古寺風物誌』(1943)・堀辰雄『大和路・信濃路』(1943)を、
奈良の寺院・仏像という**対象を固定した整列比較**で読む計量文体プロジェクト。
判別器は主役ではない。主役は実体アライメントとスタンス特徴量、成果物は三面鏡の地図(Vercel)。

読む順: 本書 → SPEC.md → IMPLEMENTATION_GUIDE.md(該当フェーズ)→ TEST_SPEC.md。

---

## プロジェクト固有規律

### 1. 引用の二層原則(最重要)

テキストは**表示層(原文逐語)と分析層(正規化済み)の二層**で持つ。
- passages.json の引用フィールドは青空文庫本文と**逐語一致**でなければならない(Q-03)。
  正規化・要約・句読点変更を加えた文字列を「引用」として出力してはならない
- 分析(形態素解析・特徴量)は正規化層に対して行う。二層の対応は文字オフセットで保持する
- 引用には底本表記(青空文庫の底本・入力者・校正者)を必ず付す

### 2. 実体辞書は curated のみ

寺・仏像の実体辞書(data/curated/entities.csv)への追加・別名登録は人間の判断で行う。
エージェントが本文から新実体や別名を「発見」した場合は、辞書に自動追加せず
needs_entity_review に出力してエスカレーションする。
同一指示解決(「中宮寺の御仏」=「半跏思惟像」)は辞書の aliases 列の明示指定のみで行う。

### 3. スタンス辞書の版管理

data/curated/lexicons/ の各辞書(比較参照・宗教語彙・感覚描写)はヘッダに version を持つ。
- 辞書の変更は専用コミット(`data: lexicon vX.Y — 理由`)で行い、機能変更と混ぜない
- 辞書を変更したループでは感度分析(変更前後の特徴量・散布図の変位)を実行し、
  結果をループログに記録する(Q-06)。測定器の設計は研究の一部であり、無断で動かさない

### 4. 捏造の絶対禁止

- 存在しない記述を実体に紐付けない。整列の確信が持てない段落は entity_id を付けず保留にする
- 引用の出典(作品・段落位置)を推測で埋めない
- 三作+承認済みベースライン以外のテキストを分析対象に加える場合は必ずエスカレーション
  (PD 確認と底本の権利確認を人間が行う)

### 5. 技術スタック規約

- パイプライン: Python 3.12。外部依存は fugashi + unidic-lite のみ(SPEC N-01)。
  pandas・sklearn は使わない(PCA は numpy なしの自前実装 or 標準ライブラリで可能な範囲、
  詳細は GUIDE §5。scikit-learn 追加が必要と判断したらエスカレーション)
- Web: Next.js 静的エクスポート + MapLibre + 地理院タイル + D3(散布図)。tsukiji-atlas と同型
- テストはフィクスチャ駆動(実青空文庫アクセスは make bronze の手動実行のみ)
- R(stylo)は P4 の任意サイドカーに限定し、本流の判定には使わない

### 6. ループ運用

- P3(整列・特徴量)と P5 の UI 開発は passages.json / sites.geojson 契約(SPEC §5)を境界に
  worktree 並走可
- loop_001(コーパス探査)完了までは SPEC の対象実体リスト・ベースライン作品は仮置き。
  探査結果による SPEC 確定は専用コミットで行う

<!-- scaffoldctl init 実行後、この下に managed block が追記される -->

<!-- scaffold:block agents_core v1.0.0 -->
## 共通規律(scaffold 管理領域 — 手動編集禁止)

このセクションはスキャフォールド・レジストリが管理する。内容を変更したい場合は、
このファイルを直接編集せず、失敗ログ → HARNESS_CHANGELOG 起票 → レジストリ改訂 → `scaffoldctl update` の経路で行うこと。

### 7 段階ループプロトコル

| 段階 | 名称 | 完了条件 |
|---|---|---|
| 1 | 計画 | 対象の要求 ID を特定し、`loop_start` を記録した |
| 2 | 文脈読込 | SPEC.md / IMPLEMENTATION_GUIDE.md の該当箇所と、直近ループのログを読んだ |
| 3 | テスト先行 | TEST_SPEC.md にトレースする失敗するテストを書き、赤を確認した |
| 4 | 実装 | ファイル編集 2 回ごとにテストを実行し、赤のまま次の編集に進んでいない |
| 5 | 検証 | 全テスト合格 + 独立再計算(該当時)を確認した |
| 6 | 文書同期 | SPEC/docs と実装の乖離(SPEC-DRIFT)を解消し、生成ドキュメントを再生成した |
| 7 | 完了 | `loop_end` を記録し、ループログ validate に合格し、専用コミットを積んだ |

### ループ可観測性

全ループは loop-observability の規律(LOOP_LOG_SPEC / FAILURE_TAXONOMY)に従い
`logs/loops/{loop_id}.jsonl` に記録する。失敗は気づいた瞬間に分類コード付きで記録する。
ツーストライク(LL-10)と S1 即時起票(LL-12)は本プロジェクトでも有効である。

### エスカレーション規範

以下の場合は作業を止め、`escalation` を記録してから人間に確認する:
仕様の複数解釈(SPEC-AMB 相当)/ スコープ外ファイルへの変更が必要になった /
破壊的操作(履歴改変・データ削除・強制 push)/ 同種の修正の 3 回目の失敗(PROC-LOOP)。

### コミット規約

Conventional Commits(feat/fix/test/docs/refactor/chore)。スキャフォールド更新は
`chore: scaffold vX.Y.Z` の専用コミットで行い、機能変更と混ぜない。
<!-- /scaffold:block agents_core -->
