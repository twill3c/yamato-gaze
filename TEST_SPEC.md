# TEST_SPEC.md — yamato-gaze

## 実行規約

- `python -m pytest -q --tb=no` を stage 3–5 と wtctl のベースライン/check に使用
- マーカー: `unit` / `integration` / `validation`。全テストはフィクスチャ駆動(N-02)
- フィクスチャは実 3 作の冒頭部+人工の合成テキストを併用(青空文庫記法の網羅は合成側で)
- フィクスチャ更新・gold_annotations 更新は専用コミット+ループログに理由記録

## パーサ(T-01x)— extract/aozora.py

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-011 | F-01 | ルビ・注記・見出しを含む合成テキスト | 段落列に構造化、注記が本文から除去され種別記録 |
| T-012 | F-02 | 二層の逐語性 | 表示層から入力原文(本文部)が完全復元できる |
| T-013 | F-02 | オフセット対応 | 分析層の任意位置から表示層の対応範囲を引ける(ルビ跨ぎ含む) |
| T-014 | F-01 | 底本フッタ | source_note(底本・入力者・校正者)が構造化される。欠落時は明示エラー |
| T-015 | F-01 | 旧字・外字注記 [#...] | 落とさず記録、分析層の文字数整合が保たれる |

## 実体タグ付け(T-02x)— transform/entities.py

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-021 | F-03 | 正式名・別名のヒット | 「中宮寺」「御寺」(aliases 指定時)が同一 entity_id に付く |
| T-022 | F-03 | 文脈規則 | 「観音」単独は親寺文脈なしでは付かず、文脈ありでは付く |
| T-023 | F-03 | 未知候補 | 辞書外の寺名らしき語が needs_entity_review に出て、タグは付かない |
| T-024 | Q-01 | gold 30 段落 | 再現率 80% / 適合率 90% 以上(gold_annotations.csv に対する機械検査) |

## 整列・特徴量(T-03x)— align.py / stance.py

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-031 | F-04 | 連続段落の併合 | 同一実体タグの連続範囲が 1 passage に、非連続は別 passage |
| T-032 | F-05 | 特徴量の手計算照合 | 短い固定段落で比較参照・宗教語彙・感覚描写・読点密度が手計算値と一致 |
| T-033 | Q-04 | lexicon_version | 全レコードに付与され、辞書ファイル先頭の version と一致。欠落は validate FAIL |
| T-034 | F-05 | 一人称率・現在形率 | fugashi 出力に対する判定規則が固定ケースで期待通り |
| T-035 | F-12 | 感度分析 | 辞書 2 版で差分レポートが生成され、変更語を含む段落だけが変位する |

## ベースライン・射影(T-04x)— analyze/

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-041 | F-06 | 変位ベクトル | 合成データ(既知の重心差)で変位が解析解と一致 |
| T-042 | F-09 | PCA 自前実装 | 既知の 2 次元構造を持つ合成データで第 1 主成分方向が解析解と一致(符号自由) |
| T-043 | F-11 | (任意)stylo 突合 | 距離順位の Spearman 相関が閾値以上(サイドカー導入時に定義) |

## Gold・validate(T-05x)

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-051 | Q-03 | 引用の逐語一致 | passages.json の quote が Bronze 本文の該当範囲と一致。1 字でも違えば FAIL |
| T-052 | Q-02 | 整列カバレッジ | 2 名以上 12 実体 / 3 名 5 実体の下限検査(フィクスチャで境界確認) |
| T-053 | Q-05 | verified 除外 | false の実体が geojson に出ず excluded 計上 |
| T-054 | F-07 | 三角測量 | counts.json の実体別 passage 合計 = passages.json 件数 = UI 用 JSON 件数を別経路で再計算し一致 |
| T-055 | §5 | 契約充足 | 必須フィールド・enum(author/type)の充足。source_note 100% |

## Web(T-06x)— P5 で詳細化

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-061 | F-08 | 著者チップの表示切替(純関数) | 選択集合 → 表示列の写像が正しい |
| T-062 | F-09 | 散布図データ整形 | xy 欠落・単著者実体の扱いが仕様通り |

## 手動確認(自動化対象外)

- Q-01 の gold_annotations 作成そのもの(30 段落の人手アノテ — P2 の人間工程)
- 三面鏡の読み比べ体験・serif 表示・底本表記(P5 チェックリスト)
- N-05: スマホ実機での初期ロード後オフライン閲覧(機内モード試験)
- P6: 現地での引用範囲・座標の妥当性(verified 更新として記録)
