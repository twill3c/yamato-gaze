# IMPLEMENTATION_GUIDE.md — yamato-gaze

## 0. ディレクトリ計画

```
yamato-gaze/
├── config/route.json                 # 巡礼路(エリア順序・地図初期表示)
├── extract/aozora.py                 # 取得+記法パース(F-01, F-02)
├── transform/
│   ├── entities.py                   # 実体タグ付け(F-03)
│   ├── align.py                      # 整列(F-04)
│   └── stance.py                     # 特徴量(F-05)+ 感度分析(F-12)
├── analyze/
│   ├── baseline.py                   # 著者ベースライン距離(F-06)
│   └── project2d.py                  # 散布図座標(§5)
├── gold/export.py, validate.py       # F-07
├── data/
│   ├── curated/entities.csv          # 実体辞書(Git 管理・verified 運用)
│   ├── curated/lexicons/*.csv        # スタンス辞書(version 付き)
│   ├── curated/gold_annotations.csv  # Q-01 用の人手アノテ 30 段落
│   ├── bronze/                       # 生テキスト(gitignore、fixtures が正)
│   └── fixtures/                     # テスト用固定データ
├── public/data/{sites.geojson, passages.json, counts.json}
├── web/                              # Next.js(P5)
├── r-sidecar/                        # stylo 検証(P4 任意、本流から隔離)
├── tests/
└── harness/                          # scaffoldctl init が配置
```

## 1. フェーズ計画

| フェーズ | ループ | 内容 | 完了条件 |
|---|---|---|---|
| P1 | loop_001 | **コーパス探査**: 3 作+ベースライン候補の青空文庫収録・底本・仮名遣い確認(分岐: 亀井が無い場合 → §7)。三作の対象実体の粗い棚卸し → SPEC §2 実体リスト確定(専用コミット) | 探査メモ+SPEC 確定 |
| P2 | loop_002 | 青空文庫パーサ(F-01, F-02)+フィクスチャ。**本案最重量** | T-01x 合格 |
| P2 | loop_003 | 実体辞書整備+タグ付け(F-03)+gold_annotations 30 段落の人手作成(人間工程) | T-02x + Q-01 |
| P3 | loop_004 | 整列(F-04)+スタンス特徴量(F-05) | T-03x + Q-02〜04 |
| P4 | loop_005 | ベースライン距離(F-06)+2 次元射影+感度分析(F-12) | T-04x |
| P4' | loop_006 | (任意)R/stylo サイドカー(F-11)。**R 入門の入口**: ISLR 原書ラボの言語に P4 の題材で触れる | 距離行列の突合レポート |
| P5 | loop_007 | Gold 出力+validate(F-07)→ 地図 UI 三面鏡+散布図(F-08, F-09)。**loop_006 以前と worktree 並走可**(契約境界: §5) | T-05x + 手動確認 |
| P5 | loop_008 | Vercel 公開(F-10)+ライセンス配置(N-04)。**締切 = 奈良行きの日程から逆算 2 週間前** | 本番 URL + スマホ確認 |
| P6 | — | 奈良で現地検証: 引用範囲・座標・三面鏡の実用性を verified 更新で反映 | verified 更新コミット |

## 2. 青空文庫パーサ(P2 の核)

- 入力: 青空文庫のテキスト版(ルビ《》、入力者注 [#...]、底本情報フッタ)
- 処理: ①ヘッダ/フッタ分離(底本表記は source_note 用に構造化保存)②注記の除去と
  種別記録 ③ルビの分離(親文字列+読み列の二層。読みは将来のモーラ分析資産)
  ④段落分割(空行・字下げ規則)⑤表示層をそのまま保持、分析層は NFKC +ルビ除去で生成し、
  **文字オフセット対応表**を持つ(AGENTS §1 の二層原則の実装)
- 逐語性の担保: 表示層から原文を復元できることをテストで保証する(T-012)
- この Parser は詩案(案6)・捕物帳案(案13)へ持ち回る共通資産として書く(実体照合や
  スタンスに依存しない純パーサに保つ)

## 3. 実体タグ付けと整列(P2–P3)

- entities.csv スキーマ: entity_id, type(temple|statue), name, aliases(|区切り),
  parent(仏像→寺), lat, lon, verified, notes
- タグ付け: 段落の表示層に対する辞書マッチ(aliases 含む)。**文脈規則**: 一般語と衝突する
  別名(「薬師」「観音」単独等)は親寺名が同段落または直前 N 段落に出現する場合のみ有効
  — 規則は entities.csv の notes に個別記載し、テストで固定する
- 曖昧・未知の候補は needs_entity_review.csv へ(辞書自動追加禁止、AGENTS §2)
- 整列: entity_id ごとに著者別の段落束を作る。連続段落は 1 passage に併合してよい
  (併合規則: 同一実体タグが連続する範囲)

## 4. スタンス特徴量(P3)

- lexicons/*.csv 形式: 1 行目 `# version: 1.0`、以降 term, note。比較標識辞書は
  正規表現可の列を許す(「〜に似」等の活用対応)
- fugashi(UniDic)で品詞・活用を取得: 一人称率(代名詞リスト)、現在形率(文末形態素の
  テンス判定は近似で可 — 判定規則を docs に明記)、知覚動詞は辞書+品詞の複合条件
- 出力は段落単位 → passages.json の features へ。lexicon_version を必ず刻む(Q-04)
- 感度分析(F-12): 辞書の新旧 2 版で特徴量を再計算し、変位の要約(実体×著者別の差分上位)を
  out/sensitivity/ に出す

## 5. ベースラインと 2 次元射影(P4)

- ベースライン: 各著者の非大和作品(loop_001 で確定)に同じ特徴量を適用し、著者ごとの
  重心を得る。「大和を書くときの変位ベクトル」= 大和段落重心 − ベースライン重心
- 射影: 特徴量は 7 次元程度なので PCA は自前実装で足りる(共分散行列の固有分解、
  標準ライブラリ+自前線形代数で可。実装が煩雑なら numpy 追加をエスカレーション —
  安易に sklearn へ逃げない)。第 1–2 主成分を xy として passages.json に書く
- R サイドカー(任意): r-sidecar/ に stylo スクリプトを置き、機能語頻度による著者間距離
  (Burrows Delta)を算出、Python 側の距離順位と Spearman 相関で突合。**本流の判定には使わない**

## 6. Web(P5)

- tsukiji-atlas の構成を流用(Next.js 静的 + MapLibre + 地理院タイル)。追加は 2 点:
  三面鏡パネル(著者チップで列の表示切替、引用は serif、底本表記を末尾に)と
  散布図ページ(D3、点=passage、色=著者、ホバーで引用先頭を表示、クリックで該当実体へ)
- ピンの大きさ = passage_count、色 = authors_count(1/2/3 名)
- N-05: データは全て静的バンドル。Service Worker までは要らないが、単一 JSON 化で
  初期ロード後の追加取得ゼロを保つ

## 7. エスカレーション対象(既知の判断待ち)

- **亀井『大和古寺風物誌』が青空文庫に無い場合**: PD なので底本からの自前入力は適法だが
  工数が大きい。選択肢 = ①自前入力(数章のみ先行)②国会図書館デジタルの本文起こし
  ③亀井を後日追加とし二面鏡で先行公開。人間が決める
- ベースライン作品の選定(ジャンルを揃えるか、代表作を取るか)
- 現在形率の判定規則の粒度(近似で開始し、誤判定が特徴量を歪めるなら精緻化)
- numpy 追加の要否(§5)
